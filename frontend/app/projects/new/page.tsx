"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { Shell } from "@/components/shell";
import { api, Project } from "@/lib/api";
import { Button, Card, Input } from "@/components/ui";

export default function NewProjectPage() { const router=useRouter(); const [error,setError]=useState(""); const [pending,setPending]=useState(false); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setPending(true);setError("");const f=new FormData(e.currentTarget);try{const project=await api<Project>("/projects/",{method:"POST",body:JSON.stringify({name:f.get("name"),code:f.get("code"),client_name:f.get("client_name"),location:f.get("location"),planned_start_date:f.get("planned_start_date")||null,planned_end_date:f.get("planned_end_date")||null,status:f.get("status")})},true);router.push(`/projects/${project.id}`)}catch(err){setError(err instanceof Error?err.message:"Unable to create project.")}finally{setPending(false)}} return <Shell><div className="mb-7"><p className="text-sm font-semibold text-moss">Projects</p><h1 className="mt-1 text-3xl font-bold">Create project</h1><p className="mt-2 text-[#65716a]">Set a clear baseline for the team.</p></div><Card className="max-w-3xl p-6"><form onSubmit={submit}>{error&&<p className="mb-5 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-medium sm:col-span-2">Project name<Input className="mt-1.5" name="name" required placeholder="Central Tower"/></label><label className="text-sm font-medium">Project code<Input className="mt-1.5" name="code" required placeholder="CT-001"/></label><label className="text-sm font-medium">Client<Input className="mt-1.5" name="client_name" placeholder="Client or organization"/></label><label className="text-sm font-medium sm:col-span-2">Site location<Input className="mt-1.5" name="location" placeholder="Tashkent, Uzbekistan"/></label><label className="text-sm font-medium">Planned start<Input className="mt-1.5" name="planned_start_date" type="date"/></label><label className="text-sm font-medium">Planned completion<Input className="mt-1.5" name="planned_end_date" type="date"/></label><label className="text-sm font-medium">Initial status<select name="status" className="mt-1.5 w-full rounded-lg border border-[#d8ded8] bg-white px-3 py-2.5 text-sm"><option value="DRAFT">Draft</option><option value="ACTIVE">Active</option></select></label></div><div className="mt-7 flex justify-end gap-3"><Button type="button" className="bg-white text-ink ring-1 ring-[#d8ded8] hover:bg-[#f7f8f6]" onClick={()=>router.back()}>Cancel</Button><Button disabled={pending}>{pending?"Creating…":"Create project"}</Button></div></form></Card></Shell>; }
