"use client";
import { FormEvent, useEffect, useState } from "react";
import { Shell } from "@/components/shell";
import { api, Company, getCompanyId, setCompanyId } from "@/lib/api";
import { useAuth } from "@/components/auth-provider";
import { Button, Card, Input } from "@/components/ui";

export default function SettingsPage(){const {companies,refresh}=useAuth();const [error,setError]=useState("");const [message,setMessage]=useState("");const current=companies.find(c=>c.id===getCompanyId());async function create(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{const company=await api<Company>("/companies/",{method:"POST",body:JSON.stringify({name:f.get("name"),slug:f.get("slug")||undefined,currency_code:f.get("currency_code"),timezone:f.get("timezone")})});setCompanyId(company.id);await refresh();setMessage("Company workspace created.")}catch(e){setError(e instanceof Error?e.message:"Unable to create company")}}async function update(e:FormEvent<HTMLFormElement>){e.preventDefault();if(!current)return;const f=new FormData(e.currentTarget);try{await api(`/companies/${current.id}/`,{method:"PATCH",body:JSON.stringify({name:f.get("name"),currency_code:f.get("currency_code"),timezone:f.get("timezone")})});await refresh();setMessage("Settings saved.")}catch(e){setError(e instanceof Error?e.message:"Unable to save settings")}}return <Shell><div className="mb-7"><p className="text-sm font-semibold text-moss">Workspace</p><h1 className="mt-1 text-3xl font-bold">Settings</h1><p className="mt-2 text-[#65716a]">Manage your company identity and operating defaults.</p></div>{error&&<p className="mb-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}{message&&<p className="mb-4 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-700">{message}</p>}<Card className="max-w-2xl p-6">{current?<form onSubmit={update} className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-medium sm:col-span-2">Company name<Input className="mt-1.5" name="name" defaultValue={current.name} required/></label><label className="text-sm font-medium">Accounting currency<select name="currency_code" defaultValue={current.currency_code} className="mt-1.5 w-full rounded-lg border border-[#d8ded8] bg-white px-3 py-2.5"><option value="UZS">UZS</option><option value="USD">USD</option></select></label><label className="text-sm font-medium">Timezone<Input className="mt-1.5" name="timezone" defaultValue="Asia/Tashkent" required/></label><div className="sm:col-span-2"><Button>Save settings</Button></div></form>:<form onSubmit={create} className="grid gap-5 sm:grid-cols-2"><div className="sm:col-span-2"><h2 className="text-xl font-bold">Create your company</h2><p className="mt-1 text-sm text-[#65716a]">You’ll become the workspace owner.</p></div><label className="text-sm font-medium sm:col-span-2">Company name<Input className="mt-1.5" name="name" required placeholder="KRCraft Construction"/></label><label className="text-sm font-medium">Workspace slug <span className="font-normal text-[#849088]">optional</span><Input className="mt-1.5" name="slug" placeholder="krcraft"/></label><label className="text-sm font-medium">Accounting currency<select name="currency_code" className="mt-1.5 w-full rounded-lg border border-[#d8ded8] bg-white px-3 py-2.5"><option value="UZS">UZS</option><option value="USD">USD</option></select></label><label className="text-sm font-medium sm:col-span-2">Timezone<Input className="mt-1.5" name="timezone" defaultValue="Asia/Tashkent" required/></label><div className="sm:col-span-2"><Button>Create workspace</Button></div></form>}</Card></Shell>}
