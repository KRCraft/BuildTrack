"use client";
import { FormEvent, useCallback, useEffect, useState } from "react";
import { Shell } from "@/components/shell";
import { api } from "@/lib/api";
import { Badge, Button, Card, Input } from "@/components/ui";
type Supplier={id:string;name:string;tax_id:string;phone:string;email:string;address:string;is_active:boolean};
export default function SuppliersPage(){const [items,setItems]=useState<Supplier[]>([]),[open,setOpen]=useState(false),[error,setError]=useState("");const load=useCallback(()=>api<Supplier[]>("/suppliers/",{},true).then(setItems).catch(e=>setError(e.message)),[]);useEffect(()=>{load()},[load]);async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{await api("/suppliers/",{method:"POST",body:JSON.stringify({name:f.get("name"),tax_id:f.get("tax_id"),phone:f.get("phone"),email:f.get("email"),address:f.get("address")})},true);setOpen(false);load()}catch(e){setError(e instanceof Error?e.message:"Unable to create supplier")}}async function deactivate(s:Supplier){if(!confirm(`Deactivate ${s.name}?`))return;try{await api(`/suppliers/${s.id}/`,{method:"PATCH",body:JSON.stringify({is_active:false})},true);load()}catch(e){setError(e instanceof Error?e.message:"Unable to update supplier")}}return <Shell><div className="mb-7 flex items-end justify-between"><div><h1 className="text-3xl font-bold">Suppliers</h1><p className="mt-2 text-[#65716a]">Maintain the vendors available for project expense records.</p></div><Button onClick={()=>setOpen(!open)}>New supplier</Button></div>{error&&<p className="mb-5 rounded-lg bg-red-50 p-4 text-red-700">{error}</p>}{open&&<Card className="mb-6 p-5"><form onSubmit={save} className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium">Supplier name<Input className="mt-1.5" name="name" required/></label><label className="text-sm font-medium">Tax ID<Input className="mt-1.5" name="tax_id"/></label><label className="text-sm font-medium">Phone<Input className="mt-1.5" name="phone"/></label><label className="text-sm font-medium">Email<Input className="mt-1.5" name="email" type="email"/></label><label className="text-sm font-medium sm:col-span-2">Address<Input className="mt-1.5" name="address"/></label><Button className="w-fit">Save supplier</Button></form></Card>}<Card><div className="overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead className="bg-[#fafbf9] text-xs uppercase tracking-wide text-[#7b8780]"><tr><th className="px-5 py-3">Supplier</th><th className="px-5 py-3">Tax ID</th><th className="px-5 py-3">Contact</th><th className="px-5 py-3">Status</th><th className="px-5 py-3"></th></tr></thead><tbody className="divide-y divide-[#edf0eb]">{items.map(s=><tr key={s.id}><td className="px-5 py-4 font-semibold">{s.name}</td><td className="px-5 py-4">{s.tax_id||"—"}</td><td className="px-5 py-4 text-[#65716a]">{s.email||s.phone||"—"}</td><td className="px-5 py-4"><Badge value={s.is_active?"ACTIVE":"INACTIVE"}/></td><td className="px-5 py-4">{s.is_active&&<button className="text-sm font-semibold text-clay" onClick={()=>deactivate(s)}>Deactivate</button>}</td></tr>)}{!items.length&&<tr><td colSpan={5} className="p-10 text-center text-[#718078]">No suppliers recorded.</td></tr>}</tbody></table></div></Card></Shell>}
