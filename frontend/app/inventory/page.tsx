"use client";
import { FormEvent, useCallback, useEffect, useState } from "react";
import Link from "next/link"; import { Shell } from "@/components/shell"; import { api } from "@/lib/api"; import { Badge, Button, Card, Input } from "@/components/ui";
type Balance={id:string;material:string;material_name:string;unit:string;location:string;location_name:string;project_name:string|null;quantity_on_hand:string;minimum_stock_level:string;low_stock:boolean};type Location={id:string;name:string;location_type:string};type Material={id:string;name:string};
export default function InventoryPage(){const [balances,setBalances]=useState<Balance[]>([]),[locations,setLocations]=useState<Location[]>([]),[materials,setMaterials]=useState<Material[]>([]),[low,setLow]=useState(false),[show,setShow]=useState(false),[error,setError]=useState("");const load=useCallback(async()=>{try{const query=low?"?low_stock=true":"";const [b,l,m]=await Promise.all([api<Balance[]>(`/inventory/balances/${query}`,{},true),api<Location[]>("/inventory/locations/",{},true),api<Material[]>("/materials/",{},true)]);setBalances(b);setLocations(l);setMaterials(m)}catch(e){setError(e instanceof Error?e.message:"Unable to load inventory")}},[low]);useEffect(()=>{load()},[load]);async function receive(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{await api("/inventory/receipts/",{method:"POST",body:JSON.stringify({location:f.get("location"),material:f.get("material"),quantity:f.get("quantity"),reason:f.get("reason")})},true);setShow(false);load()}catch(e){setError(e instanceof Error?e.message:"Unable to receive stock")}}return <Shell><div className="mb-7 flex items-end justify-between"><div><h1 className="text-3xl font-bold">Inventory</h1><p className="mt-2 text-[#65716a]">Stock by warehouse and project site. Low stock uses company-wide material totals.</p></div><div className="flex gap-3"><Link href="/inventory/transfers/new" className="rounded-lg bg-white px-4 py-2.5 text-sm font-semibold ring-1 ring-[#d8ded8]">Transfer material</Link><Button onClick={()=>setShow(!show)}>Receive stock</Button></div></div>{error&&<p className="mb-4 rounded-lg bg-red-50 p-4 text-red-700">{error}</p>}{show&&<Card className="mb-6 p-5"><form onSubmit={receive} className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><select name="location" className="rounded-lg border border-[#d8ded8] p-2.5" required><option value="">Destination location</option>{locations.map(l=><option key={l.id} value={l.id}>{l.name}</option>)}</select><select name="material" className="rounded-lg border border-[#d8ded8] p-2.5" required><option value="">Material</option>{materials.map(m=><option key={m.id} value={m.id}>{m.name}</option>)}</select><Input name="quantity" type="number" min="0.0001" step="0.0001" placeholder="Quantity" required/><Input name="reason" placeholder="Delivery note"/><Button className="w-fit">Record receipt</Button></form></Card>}<Card><div className="flex gap-3 border-b border-[#edf0eb] p-4"><label className="flex items-center gap-2 text-sm font-medium"><input type="checkbox" checked={low} onChange={e=>setLow(e.target.checked)}/> Low stock only</label></div><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-[#fafbf9] text-xs uppercase tracking-wide text-[#7b8780]"><tr><th className="px-5 py-3">Material</th><th className="px-5 py-3">Location</th><th className="px-5 py-3">Project</th><th className="px-5 py-3">On hand</th><th className="px-5 py-3">Minimum</th><th className="px-5 py-3">Status</th></tr></thead><tbody className="divide-y divide-[#edf0eb]">{balances.map(b=><tr key={b.id}><td className="px-5 py-4 font-semibold">{b.material_name}<span className="ml-2 text-xs text-[#78847d]">{b.unit}</span></td><td className="px-5 py-4">{b.location_name}</td><td className="px-5 py-4">{b.project_name||"—"}</td><td className="px-5 py-4">{b.quantity_on_hand}</td><td className="px-5 py-4">{b.minimum_stock_level}</td><td className="px-5 py-4"><Badge value={b.low_stock?"LOW STOCK":"IN STOCK"}/></td></tr>)}{!balances.length&&<tr><td colSpan={6} className="p-10 text-center text-[#718078]">No inventory balances yet.</td></tr>}</tbody></table></div></Card></Shell>}
