"use client";
import { FormEvent, useCallback, useEffect, useState } from "react";
import { Shell } from "@/components/shell"; import { api } from "@/lib/api"; import { Badge, Button, Card, Input } from "@/components/ui";
type Material={id:string;code:string;name:string;category:string;unit:string;minimum_stock_level:string;total_stock:string;low_stock:boolean;is_active:boolean};
export default function MaterialsPage(){const [items,setItems]=useState<Material[]>([]),[show,setShow]=useState(false),[error,setError]=useState("");const load=useCallback(()=>api<Material[]>("/materials/",{},true).then(setItems).catch(e=>setError(e.message)),[]);useEffect(()=>{load()},[load]);async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{await api("/materials/",{method:"POST",body:JSON.stringify({code:f.get("code"),name:f.get("name"),category:f.get("category"),unit:f.get("unit"),minimum_stock_level:f.get("minimum_stock_level")||0})},true);setShow(false);load()}catch(e){setError(e instanceof Error?e.message:"Unable to save material")}}async function deactivate(id:string){if(!confirm("Deactivate this material?"))return;await api(`/materials/${id}/`,{method:"PATCH",body:JSON.stringify({is_active:false})},true);load()}return <Shell><div className="mb-7 flex items-end justify-between"><div><h1 className="text-3xl font-bold">Materials</h1><p className="mt-2 text-[#65716a]">Standardized construction materials and company-wide stock thresholds.</p></div><Button onClick={()=>setShow(!show)}>New material</Button></div>{error&&<p className="mb-4 rounded-lg bg-red-50 p-4 text-red-700">{error}</p>}{show&&<Card className="mb-6 p-5"><form onSubmit={save} className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Input name="code" placeholder="Material code" required/><Input name="name" placeholder="Material name" required/><Input name="category" placeholder="Category"/><select name="unit" className="rounded-lg border border-[#d8ded8] bg-white px-3"><option>PIECE</option><option>KG</option><option>TON</option><option>METER</option><option>M2</option><option>M3</option><option>LITER</option><option>BAG</option><option>BOX</option><option>OTHER</option></select><Input name="minimum_stock_level" type="number" min="0" step="0.0001" placeholder="Minimum stock"/><Button className="w-fit">Save material</Button></form></Card>}<Card><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-[#fafbf9] text-xs uppercase tracking-wide text-[#7b8780]"><tr><th className="px-5 py-3">Material</th><th className="px-5 py-3">Category</th><th className="px-5 py-3">Unit</th><th className="px-5 py-3">Total stock</th><th className="px-5 py-3">Minimum</th><th className="px-5 py-3">Status</th><th/></tr></thead><tbody className="divide-y divide-[#edf0eb]">{items.map(m=><tr key={m.id}><td className="px-5 py-4 font-semibold">{m.name}<span className="ml-2 text-xs text-[#78847d]">{m.code}</span></td><td className="px-5 py-4">{m.category||"—"}</td><td className="px-5 py-4">{m.unit}</td><td className="px-5 py-4">{m.total_stock}</td><td className="px-5 py-4">{m.minimum_stock_level}</td><td className="px-5 py-4"><Badge value={m.low_stock?"LOW STOCK":m.is_active?"ACTIVE":"INACTIVE"}/></td><td className="px-5 py-4">{m.is_active&&<button onClick={()=>deactivate(m.id)} className="text-sm font-semibold text-clay">Deactivate</button>}</td></tr>)}{!items.length&&<tr><td colSpan={7} className="p-10 text-center text-[#718078]">No materials yet.</td></tr>}</tbody></table></div></Card></Shell>}
