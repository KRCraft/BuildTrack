"use client";
import { FormEvent, useCallback, useEffect, useState } from "react";
import { Mail, UserPlus } from "lucide-react";
import { Shell } from "@/components/shell";
import { api, getCompanyId } from "@/lib/api";
import { Badge, Button, Card, Input } from "@/components/ui";

type Member={id:string;role:string;status:string;user:{first_name:string;last_name:string;email:string}};
export default function TeamPage(){const [members,setMembers]=useState<Member[]>([]),[error,setError]=useState(""),[showInvite,setShowInvite]=useState(false);const companyId=getCompanyId();const load=useCallback(()=>companyId&&api<Member[]>(`/companies/${companyId}/members/`).then(setMembers).catch(e=>setError(e.message)),[companyId]);useEffect(()=>{load()},[load]);async function invite(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{await api(`/companies/${companyId}/invitations/`,{method:"POST",body:JSON.stringify({email:f.get("email"),role:f.get("role")})});setShowInvite(false);alert("Invitation sent.")}catch(e){setError(e instanceof Error?e.message:"Unable to invite member")}}async function changeRole(member:Member,role:string){try{await api(`/companies/${companyId}/members/${member.id}/`,{method:"PATCH",body:JSON.stringify({role})});load()}catch(e){setError(e instanceof Error?e.message:"Unable to update member")}}return <Shell><div className="mb-7 flex items-end justify-between"><div><p className="text-sm font-semibold text-moss">Workspace</p><h1 className="mt-1 text-3xl font-bold">Team</h1><p className="mt-2 text-[#65716a]">Invite the people accountable for delivery.</p></div><Button onClick={()=>setShowInvite(!showInvite)}><UserPlus className="mr-2 inline" size={16}/>Invite member</Button></div>{error&&<p className="mb-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}{showInvite&&<Card className="mb-6 p-5"><form onSubmit={invite} className="flex flex-col gap-3 sm:flex-row sm:items-end"><label className="flex-1 text-sm font-medium">Email<Input className="mt-1.5" type="email" name="email" required placeholder="colleague@company.com"/></label><label className="text-sm font-medium">Role<select name="role" className="mt-1.5 rounded-lg border border-[#d8ded8] bg-white px-3 py-2.5"><option value="PROJECT_MANAGER">Project manager</option><option value="SITE_MANAGER">Site manager</option><option value="ACCOUNTANT">Accountant</option></select></label><Button><Mail className="mr-2 inline" size={15}/>Send invite</Button></form></Card>}<Card><div className="overflow-x-auto"><table className="w-full min-w-[640px] text-left text-sm"><thead className="bg-[#fafbf9] text-xs uppercase tracking-wide text-[#7b8780]"><tr><th className="px-5 py-3">Member</th><th className="px-5 py-3">Role</th><th className="px-5 py-3">Status</th><th className="px-5 py-3">Manage</th></tr></thead><tbody className="divide-y divide-[#edf0eb]">{members.map(m=><tr key={m.id}><td className="px-5 py-4"><p className="font-semibold">{m.user.first_name} {m.user.last_name}</p><p className="mt-1 text-xs text-[#7b8780]">{m.user.email}</p></td><td className="px-5 py-4"><Badge value={m.role}/></td><td className="px-5 py-4"><Badge value={m.status}/></td><td className="px-5 py-4"><select aria-label={`Role for ${m.user.email}`} value={m.role} onChange={e=>changeRole(m,e.target.value)} className="rounded-md border border-[#d8ded8] bg-white px-2 py-1.5 text-xs"><option value="OWNER">Owner</option><option value="PROJECT_MANAGER">Project manager</option><option value="SITE_MANAGER">Site manager</option><option value="ACCOUNTANT">Accountant</option></select></td></tr>)}{!members.length&&<tr><td colSpan={4} className="p-10 text-center text-[#718078]">No members available.</td></tr>}</tbody></table></div></Card></Shell>}
